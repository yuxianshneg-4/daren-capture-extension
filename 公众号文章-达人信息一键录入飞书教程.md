# 我写了个浏览器插件：达人资料一键进飞书表格，飞书配置全过程公开（纯小白版）

## 写在前面

做达人对接的朋友都懂：在精选联盟看上一个达人，昵称、粉丝量、GMV、商品数、店铺数、类目、抖音号、微信号、主页链接……十几项信息一项项复制粘贴进表格，一个达人至少三五分钟，一天看几十个达人，光录入就废掉了。

所以我写了一个 Chrome 浏览器插件：打开达人详情页，点一下按钮，插件自动把页面上 16 项信息抓下来、自动写进你自己的飞书多维表格，全程约 12 秒。

工具已开源，文末有下载地址。这篇先不讲插件本身，把大家最容易卡住的**飞书配置全过程**讲透——照着做，10 分钟搞定，全程不需要写一行代码，也不需要买服务器。

## 先搞清楚原理（30 秒）

整个数据链路是这样的：

> 浏览器插件 → 飞书开放平台官方接口 → 你的多维表格

插件用的是飞书官方 API，靠一个你自己创建的「飞书应用」来通行。App ID 和 App Secret 保存在你自己的浏览器里，数据直接从你的浏览器写进你的表格，**不经过任何第三方服务器**，也没有月费。

你要做的就三件事：**建一张表格、建一个飞书应用、把四个凭证填进插件**。

## 第一步：建一张多维表格（5 分钟）

1. 打开飞书，新建一个「多维表格」，名字随意，比如「达人数据库」
2. 进入表格后，在第一个数据表里按下面的清单建列。注意：**列名必须一字不差**，插件是按名字匹配的

| 列名 | 字段类型 | 说明 |
|------|----------|------|
| 达人昵称 | 文本 | 必填 |
| 平台 | 单选 | 加一个选项：抖音 |
| 粉丝量 | 数字 | 「98.2万」会自动换算成 982000 |
| 粉丝量级 | 单选 | 依次加：1万以下、1-5万、5-10万、10-50万、50万以上 |
| 省份 | 单选 | 按需加省份名，如 河南 |
| 达人等级 | 单选 | Lv1 到 Lv5 |
| 月GMV | 单选 | 区间文本，如 0-250、250-500 |
| 平均单价 | 数字 | |
| 商品数 | 数字 | 取带货分析页的数据 |
| 店铺数 | 数字 | 取带货分析页的数据 |
| 一级类目 | 多选 | 按你的业务加类目名 |
| 主推产品 | 多选 | 同上 |
| 账号ID | 文本 | 存抖音号 |
| 微信号 | 文本 | |
| 主页链接 | 文本 | 查重的依据 |
| 账号详细 | 文本 | 存达人简介 |
| 建联日期 | 日期 | 插件自动填当天 |

一句话原理：插件抓到页面数据后，会拿数据和表格选项做匹配，匹配上才自动选。所以「单选/多选」列的选项名，尽量和页面上的叫法一致。

## 第二步：创建飞书应用（3 分钟）

1. 浏览器打开 open.feishu.cn（飞书开放平台），用你的飞书账号登录
2. 右上角进入「开发者后台」
3. 点「创建企业自建应用」，名字随意（比如「达人录入」），描述可不填，创建成功后自动进入应用页面
4. 左侧菜单找到「凭证与基础信息」，能看到两串字符：**App ID** 和 **App Secret**。Secret 点开只显示一次，先复制保存好

这就是插件的「身份证」。等下一共要填四个东西：App ID、App Secret，加上第一步表格的两个地址参数，缺一不可。

## 第三步：开通权限（1 分钟）

1. 还是在应用页面，左侧菜单「权限管理」
2. 搜索框输入 `bitable`
3. 找到「**查看、评论、编辑和管理多维表格**」这一项，点开通

就这一个权限，够用了。它决定了插件能读你表格的字段选项（下拉框自动加载）、能查重、能新增和更新记录。不需要开通任何其他权限，也不需要审批流权限。

## 第四步：发布应用（1 分钟）

1. 左侧菜单「版本管理与发布」→「创建版本」
2. 版本号随便填个 1.0.0，更新说明随便写
3. 「可用范围」选你自己（或全员）
4. 提交发布

注意：如果你不是企业的飞书管理员，提交后会进入审核，需要管理员在管理后台点一下通过。个人用的飞书（自己就是管理员）基本秒过。

发布成功的标志：应用状态变成「已启用」。

## 第五步：取表格的两个地址参数（1 分钟）

用浏览器打开你第一步建的多维表格，看地址栏：

```
https://xxx.feishu.cn/base/GlClbxxxxxxxxx?table=tblUxxxxxxxxx&view=vewxxxxx
```

- `/base/` 后面那串（到问号为止）就是 **Base Token**
- `table=` 后面那串（到 & 符号为止）就是**表 ID**

复制出来，和 App ID、App Secret 放在一起，你的四件套就齐了。

## 第六步：装插件、填凭证（2 分钟）

1. 到文末开源地址，下载 Release 里的 zip，解压到一个固定位置（装完别移动别删除）
2. Chrome 地址栏输入 `chrome://extensions` 回车，打开右上角「开发者模式」
3. 点「加载已解压的扩展程序」，选中解压出来的 extension 文件夹
4. 右键浏览器右上角的插件图标 → 选项，把四件套填进去，保存

验证方法：打开任意一个精选联盟达人详情页，插件侧边面板里，省份、达人等级这些下拉框能加载出你表格里的选项，就是通了。

## 日常怎么用（3 次点击）

1. 打开达人详情页，插件会静默识别页面上直接可见的信息
2. 这个达人你觉得行，点「开始捕获录入」——插件自动点开二维码弹窗抓抖音号、点主页按钮抓链接、点亮微信号、把概览、场景分析、粉丝分析、评价详情、带货分析五个标签页挨个点一遍抓全数据
3. 核对一下字段（类目这些需要你人工判断的手动选），点「确认录入」，数据进表

已有同链接的达人会自动查重，问你是「更新」还是「新建」，不会重复录入。

## 常见问题

- **填了凭证没反应**：检查应用是否已发布启用（第四步），权限是否开通（第三步）
- **提交报错，提示某个列名不存在**：回表格对照第一步的清单，列名要一字不差
- **微信号偶尔为空**：手动点一下页面微信号旁的眼睛图标，再点插件的「重新识别」
- **类目没有自动选中**：表格里的类目选项名和页面上的叫法不一致，补上同名选项即可

## 最后

- 开源地址：https://github.com/yuxianshneg-4/daren-capture-extension （含完整教程和安装包下载）
- 免责说明：工具按现状开源（MIT），仅供个人学习与工作效率辅助，采集与录入的数据用途由使用者自行负责，请遵守目标网站服务条款及相关法规，与抖音、飞书官方无关联
- 有问题欢迎留言，看到都会回

---

作者：少昱 · 转载请保留出处
